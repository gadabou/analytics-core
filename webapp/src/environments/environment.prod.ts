// import { version } from '../../package.json';
export const environment = {
  production: true,
  title: 'Production Environment Heading',
  // apiURL: 'https://localhost:9999/api',
  // appVersion: version
};
